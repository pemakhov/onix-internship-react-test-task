import React from 'react';

const Header = () => {
  const mainHeader = 'Lesson 15';
  return (
    <header className="container">
      <h1>{mainHeader}</h1>
    </header>
  );
};

export default Header;
